<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">
    <persistence-unit name="helloJpa">
        <class>com.app.kidz.domain.Member</class> <!-- 엔티티 클래스 패키지 경로 -->
        <class>com.app.kidz.domain.Order</class> <!-- 엔티티 클래스 패키지 경로 -->
        <class>com.app.kidz.domain.Book</class> <!-- 엔티티 클래스 패키지 경로 -->
        <class>com.app.kidz.domain.Item</class> <!-- 엔티티 클래스 패키지 경로 -->
        <class>com.app.kidz.domain.OrderItem</class> <!-- 엔티티 클래스 패키지 경로 -->
        <properties>
            <!-- 필수 속성 -->
            <property name="javax.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/> <!-- JDBC 드라이버 클래스 -->
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/jpa_test"/> <!-- DB 접속 URL -->
            <property name="javax.persistence.jdbc.user" value="root"/> <!-- DB 접속 계정 이름 -->
            <property name="javax.persistence.jdbc.password" value="1234"/> <!-- DB 접속 계정 비밀번호 -->
            <!-- 옵션 -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/> <!-- Hibernate 방언 설정 -->
            <property name="hibernate.show_sql" value="true"/> <!-- 쿼리 로그 출력 여부 -->
            <property name="hibernate.format_sql" value="true"/> <!-- 쿼리 로그를 보기 좋게 포맷팅할지 여부 -->
            <property name="hibernate.jdbc.batch_size" value="10"/> <!-- 쿼리를 버퍼링(모아서) 한번에 보내준다. -->
            <property name="hibernate.hbm2ddl.auto" value="create"/>
        </properties>
    </persistence-unit>
</persistence>